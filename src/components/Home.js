import Game from "./Game";
import HelpButton from "./HelpButton";

const Home = () => {
    return(
        <main>
            <h2>Home</h2>
            <Game />
            <HelpButton />
        </main>
    );
};

export default Home;